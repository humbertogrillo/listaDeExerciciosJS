// Altere a função menorInt para receber um array com números inteiros
// e retorna o menor número encontrado.
// OBS: Tente não usar Math.min()

function menorInt(arr) {
    
    return 0;
}

module.exports = {menorInt}